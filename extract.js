const fs = require('fs'); const css = fs.readFileSync('src/app/globals.css', 'utf8'); const colorRegex = /--color-([\w-]+):\s*(#[0-9a-fA-F]{6});/g; let match; const colors = []; while ((match = colorRegex.exec(css)) !== null) { const name = match[1]; const hex = match[2]; const r = parseInt(hex.slice(1, 3), 16); const g = parseInt(hex.slice(3, 5), 16); const b = parseInt(hex.slice(5, 7), 16); colors.push({ name, hex, rgb: 'rgb(' + r + ', ' + g + ', ' + b + ')' }); } console.log('| Variável Tailwind | HEX | RGB |'); console.log('|---|---|---|'); colors.forEach(c => console.log('| ' + c.name + ' | ' + c.hex + ' | ' + c.rgb + ' |'));
